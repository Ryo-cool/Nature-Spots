# I18n Requirements Rules

このルールはNature-Spotsプロジェクトの多言語対応（i18n）に関する必須要件を定義します。コード生成時およびレビュー時に自動的に適用されます。

## 適用タイミング

- **コード生成時**: UIテキストを含むコードを生成する際、必ず `$t()` を使用
- **コードレビュー時**: ハードコードされたテキストを検出して警告
- **動的リロード**: このファイルを変更すると自動的に再読み込み

---

## 🔴 Critical: 必ず守るべきルール

### 1. UIテキストは必ず `$t()` を使用

**対象**: ユーザーに表示される全てのテキスト

```vue
<!-- ❌ 禁止: ハードコードされたテキスト -->
<template>
  <v-btn>ログイン</v-btn>
  <h1>スポット一覧</h1>
  <p>エラーが発生しました</p>
</template>

<!-- ✅ 必須: $t() を使用 -->
<template>
  <v-btn>{{ $t('auth.login') }}</v-btn>
  <h1>{{ $t('spot.list.title') }}</h1>
  <p>{{ $t('errors.general') }}</p>
</template>
```

### 2. 翻訳キーは日英両方に必ず存在

**違反例**: `ja.json` にキーがあるが `en.json` にない

```json
// ❌ 禁止
// front/locales/ja.json
{
  "spot": {
    "new_feature": "新機能"
  }
}

// front/locales/en.json
{
  "spot": {
    // "new_feature" が欠けている！
  }
}
```

**修正例**:
```json
// ✅ 必須: 両方のファイルに同じキー構造
// front/locales/ja.json
{
  "spot": {
    "new_feature": "新機能"
  }
}

// front/locales/en.json
{
  "spot": {
    "new_feature": "New Feature"
  }
}
```

### 3. 動的テキストはプレースホルダーを使用

**ユーザー名、数値、日付などの変数を含むテキスト**

```vue
<!-- ❌ 禁止: テンプレートリテラル -->
<template>
  <p>{{ `ようこそ、${userName}さん` }}</p>
  <p>{{ `${count}件のスポット` }}</p>
</template>

<!-- ✅ 必須: プレースホルダー -->
<template>
  <p>{{ $t('welcome.message', { name: userName }) }}</p>
  <p>{{ $t('spot.count', { count }) }}</p>
</template>

<!-- front/locales/ja.json -->
{
  "welcome": {
    "message": "ようこそ、{name}さん"
  },
  "spot": {
    "count": "{count}件のスポット"
  }
}

<!-- front/locales/en.json -->
{
  "welcome": {
    "message": "Welcome, {name}"
  },
  "spot": {
    "count": "{count} spots"
  }
}
```

---

## 🟠 High: 強く推奨されるルール

### 4. 翻訳キーの命名規則

**構造**: `{カテゴリ}.{サブカテゴリ}.{具体的なキー}`

**良い例**:
```typescript
$t('auth.login.title')           // 認証 > ログイン > タイトル
$t('auth.login.button')          // 認証 > ログイン > ボタン
$t('spot.form.name_label')       // スポット > フォーム > 名前ラベル
$t('spot.form.name_placeholder') // スポット > フォーム > プレースホルダー
$t('errors.validation.required') // エラー > バリデーション > 必須
$t('errors.validation.max_length') // エラー > バリデーション > 最大長
```

**悪い例**:
```typescript
// ❌ 禁止: 曖昧すぎる
$t('text1')
$t('message')
$t('label')

// ❌ 禁止: 長すぎる（3階層まで）
$t('user.profile.settings.account.security.password.change.button')

// ❌ 禁止: キャメルケースではなくスネークケース
$t('auth.loginButton')  // NG
$t('auth.login_button') // OK
```

### 5. コンポーネント内での useI18n() の使用

**スクリプト内でi18nを使う場合**

```vue
<script setup lang="ts">
import { useI18n } from 'vue-i18n'

const { t, locale } = useI18n()

// ✅ OK: t() を使用
const errorMessage = computed(() => {
  return error.value ? t('errors.general') : ''
})

// 言語切り替え
const switchLanguage = () => {
  locale.value = locale.value === 'ja' ? 'en' : 'ja'
}
</script>
```

### 6. TypeScript型定義で翻訳キーを型安全に

**推奨**: 翻訳キーの型定義（大規模プロジェクト向け）

```typescript
// types/i18n.ts
export type TranslationKeys =
  | 'auth.login'
  | 'auth.logout'
  | 'spot.list.title'
  | 'errors.general'

// 使用時に型チェック
const key: TranslationKeys = 'auth.login'
$t(key)
```

---

## 🟡 Medium: 推奨されるルール

### 7. ロケールファイルの整理

**推奨構造**:
```json
{
  "common": {
    "submit": "送信",
    "cancel": "キャンセル",
    "save": "保存",
    "delete": "削除"
  },
  "auth": {
    "login": "ログイン",
    "logout": "ログアウト",
    "register": "新規登録"
  },
  "spot": {
    "title": "スポット",
    "list": {
      "title": "スポット一覧",
      "empty": "スポットがありません"
    },
    "form": {
      "name_label": "スポット名",
      "name_placeholder": "例: 富士山"
    }
  },
  "errors": {
    "general": "エラーが発生しました",
    "network": "ネットワークエラー",
    "validation": {
      "required": "必須項目です",
      "max_length": "最大{max}文字まで"
    }
  }
}
```

### 8. 複数形の処理

**Vue I18n の複数形対応**

```json
// front/locales/ja.json
{
  "spot": {
    "review": "{count}件のレビュー"
  }
}

// front/locales/en.json
{
  "spot": {
    "review": "no reviews | {count} review | {count} reviews"
  }
}
```

```vue
<template>
  <!-- 複数形に対応 -->
  <p>{{ $tc('spot.review', reviewCount) }}</p>
</template>
```

### 9. 日付・数値のフォーマット

**Vue I18n の組み込み機能を使用**

```vue
<script setup lang="ts">
import { useI18n } from 'vue-i18n'

const { d, n } = useI18n()

// 日付フォーマット
const formattedDate = computed(() => d(new Date(), 'long'))

// 数値フォーマット
const formattedPrice = computed(() => n(1000, 'currency'))
</script>

<template>
  <p>{{ formattedDate }}</p>
  <p>{{ formattedPrice }}</p>
</template>
```

---

## 許可される例外

### 1. 技術的な文字列（翻訳不要）
```typescript
// ✅ OK: ログメッセージ（開発者向け）
console.error('API request failed:', error)

// ✅ OK: API エンドポイント
const endpoint = '/api/v1/spots'

// ✅ OK: 定数名
const MAX_RETRY_COUNT = 3
```

### 2. 外部ライブラリのテキスト
```vue
<!-- ✅ OK: Vuetify が提供するデフォルトテキスト -->
<v-data-table :items="items" />
```

### 3. 開発環境限定の文字列
```typescript
// ✅ OK: 開発環境のデバッグ表示
if (import.meta.dev) {
  console.log('Debug: User data loaded')
}
```

---

## 違反時の対応

### コード生成時
- UIテキストを含むコードを生成する際は、自動的に `$t()` を使用
- 新しい翻訳キーが必要な場合は、`ja.json` と `en.json` の両方に追加

### コードレビュー時

| 重要度 | 違反内容 | 対応 |
|--------|---------|------|
| 🔴 Critical | ハードコードされたUIテキスト | 即座に修正必須 |
| 🔴 Critical | 片方の言語にのみ存在する翻訳キー | 即座に修正必須 |
| 🟠 High | 命名規則違反の翻訳キー | マージ前の修正を推奨 |
| 🟡 Medium | 動的テキストのテンプレートリテラル | マージ前の修正を推奨 |

---

## チェックリスト

新しいUIコンポーネントを追加する際の確認項目：

- [ ] 表示テキストは全て `$t()` または `useI18n()` の `t()` を使用しているか
- [ ] 新しい翻訳キーは `ja.json` と `en.json` の両方に追加したか
- [ ] 翻訳キーの命名規則（`{カテゴリ}.{サブカテゴリ}.{具体的なキー}`）に従っているか
- [ ] 動的テキスト（ユーザー名、数値など）はプレースホルダーを使用しているか
- [ ] 複数形が必要な場合、適切に対応しているか

---

## ツール

### 翻訳キーの整合性チェック
```bash
# カスタムコマンドで確認
/check-i18n
```

### 手動チェック
```bash
# ハードコードされた日本語の検出
cd front
grep -r ">[ぁ-んァ-ヶ一-龠]" components/ pages/

# $t() を使っていないコンポーネントを検索
grep -rL "\$t(" components/
```

---

## 参考リンク

- [Vue I18n Documentation](https://vue-i18n.intlify.dev/)
- [Nuxt I18n Module](https://i18n.nuxtjs.org/)
- [プロジェクトのロケールファイル](front/locales/)
